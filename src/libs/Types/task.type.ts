export type TaskType = {
    id: number;
    title: string;
    date: string;
    dueDate: string;
    description: string;
    objective: string;
    status: string;
}

export type Bookmark = {
    id: number;
    title: string;
    value: string;
}